machine:
  node:
    version: 6.9.2
  services:
    - docker

# deployment:
#   production:
#     branch: master
#     commands:
#       - docker build -t $AWS_ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/kit_api/api .
#       - sh ./deploy/circle_ci_deploy.sh
